{
    "name": "topseller",
    "private": true,
    "scripts": {
        "build": "weboops --mode build --publicPath /topseller/ -o ./docs/",
        "dev": "weboops --mode development -o ./docs/"
    },
    "dependencies": {
        "@weboops/core": "^0.2.0"
    }
}
